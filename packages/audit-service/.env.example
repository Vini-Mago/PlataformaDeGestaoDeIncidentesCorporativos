AUDIT_DATABASE_URL=postgresql://pgic:pgic@localhost:5432/audit_service
AUDIT_SERVICE_PORT=3009
BASE_URL=http://localhost:3009
# Use a unique, cryptographically secure secret for this service (do not reuse identity-service secret).
# Generate one with: openssl rand -base64 32. Min 32 chars; base64 recommended.
JWT_SECRET=change-me-in-production-use-a-unique-secret-for-audit-service
